# 2026-07-21 13:48 · redesign+F1-F5 · sessiya checkpoint

## Muhit blokeri (HAMMA F-vazifaga taalluqli)
Bulutli sandbox Go/Flutter'ni KOMPILYATSIYA/RUN qila olmaydi (proxy.golang.org,
storage.googleapis.com, pub.dev firewall; Mac toolchain yetib bo'lmaydi;
device_bash tarmoqsiz). Shu bois: smoke, go test, flutter test, emulyator,
scheduler clock-injection, TG yetkazish, screenshot — HECH BIRI bajarilmadi.
Statik tekshiruv (struktura + symbol resolution) + foydalanuvchi `flutter analyze`
yagona verifikatsiya kanali.

## Bajarilgan (statik tekshirilgan, Mac'ga commit qilingan)
- F3 · Shogirdlar ro'yxati soddalashtirildi: rich karta (~190px) → kompakt qator
  (~64px), ring→nuqta, bitta ikkilamchi qator, kompakt to'lov amali. ~3x ko'proq
  item/ekran. Fayl: features/students/ui/students_screen.dart.
- F2 · Tavsiyalar UI: recommendations_provider.dart (dio ustida, GET/POST/DELETE/
  broadcast/products), recommendations_view.dart (tab ro'yxati + qo'shish sheet +
  mahsulot pikeri + kategoriya chiplar + matn counter + broadcast switch + haptic
  lightImpact), profilga "Tavsiyalar" tab qo'shildi. Backend endpointlari MAVJUD.
- Oldingi: butun redizayn (tema light/navy, dashboard, students, profile, calendar,
  notifications, 4-tab nav, settings/obuna). Foydalanuvchi `flutter analyze` → 9
  muammo topildi va HAMMASI tuzatildi (duplicate strings, valueOrNull→value, imports).

## F1 AUDIT (BACKEND TOUCHED — o'qildi, kod yozilmadi)
Recommendations domeni CT0 tomonidan ~90% qurilgan:
- Routes (cmd/api/router.go:128-135): GET/POST /students/{id}/recommendations,
  POST /recommendations/broadcast — MAVJUD.
- internal/client/service_market.go: CreateRecommendation(107), StudentRecommendations
  (144), Products(299). repo_pg.go: InsertRecommendation(376), Recommendations(348).
- RBAC test: service_test.go (begona shogird tavsiyasi ko'rinmaydi).
- openapi: /students/{id}/recommendations, /recommendations/broadcast, Recommendation,
  RecommendationCreate, PagedRecommendations, RecommendationCategory(sportpit/water/
  sleep/nutrition/training/other) — MAVJUD.
- Seed: seedMarket + insertRecommendations + merchants + products — MAVJUD.

### F1 YETISHMAYDIGAN 3 TESHIK (yozilmadi — sabab: Go compile-verifikatsiya mumkin emas,
### working backendga ko'r Go qo'shish `go build` ni buzishi mumkin):
1. DELETE /recommendations/{id} — yo'q. Kerak:
   - repo_pg.go: `func (r *PGRepo) DeleteRecommendation(ctx, trainerID, recID string) (bool, error)`
     → `DELETE FROM recommendations WHERE id=$1 AND trainer_id=$2` (faqat o'ziniki; RowsAffected>0).
   - repo.go interfeysga qo'sh: `DeleteRecommendation(ctx, trainerID, recID string) (bool, error)`.
   - service_market.go: `func (s *Service) DeleteRecommendation(ctx, trainerID, recID string) error`
     → repo chaqir; false→notFound().
   - handler.go: `func (h *TrainerHandler) DeleteRecommendation(w,r){ err:=h.svc.DeleteRecommendation(r.Context(), trainerID(r), chi.URLParam(r,"id")); h.write(w, 204, nil, err) }`.
   - router.go (broadcast yonida): `r.Delete("/recommendations/{id}", h.clientTrainer.DeleteRecommendation)`.
   - openapi: DELETE /recommendations/{id} → 204/404/401.
   - test: t2 tavsiyasini t1 o'chira olmaydi (404); o'ziniki 204.
2. recommendation_new notification — yo'q. CreateRecommendation notify chaqirmaydi.
   - Notifier interfeysiga (service.go:15) qo'sh: `QueueStudentRecommendation(ctx, studentID, trainerName, textPreview string) error`.
   - notify/service.go: Enqueue orqali template `recommendation_new`, dedup_key YO'Q (qo'lda amal).
   - templates/uz.toml + ru.toml: `recommendation_new = "{trainer} yangi tavsiya qo'shdi: {text}"`.
   - service_market.go CreateRecommendation ichida: studentID!=nil && student.TGChatID!=nil bo'lsa
     QueueStudentRecommendation chaqir (xato bo'lsa tavsiya BEKOR qilinmaydi — log). Broadcast'da
     throttle (har shogirdga emas, yoki LastBroadcastAt hurmat).
3. Trainer product picker — GET /market/products 501 (internal/market/handler.go:35).
   - Yo yangi trainer route `GET /market/products` → repo.Products(trainerID, category, q, page, limit)
     bilan real javob (repo allaqachon bor), yoki client ListProducts ni trener uchun ochish.
   - openapi 501→200 + q/category query params.
   - F2 mahsulot pikeri bu endpointni chaqiradi (hozir 501→bo'sh, graceful).

## F4 AUDIT (BACKEND TOUCHED — o'qildi)
To'lov notification tsikli asosan qurilgan:
- internal/notify/schedule.go — scheduler (due_3days/due_today/overdue generatsiya).
- dedup: notifications.dedup_key UNIQUE "{recipient}:{template}:{date}", ON CONFLICT DO NOTHING (repo_pg.go:40).
- Templates (uz+ru): payment_due_3days, payment_due_today, payment_due_manual (shogird);
  trainer_payment_due_today, trainer_payment_overdue, trainer_daily_digest (trener).
- Manual remind: POST /students/{id}/remind (student.Remind) — MAVJUD.
- YETISHMAYDIGAN (runtime tekshiruv talab qiladi, bu sessiyada BAJARILMADI):
  clock-injection sinov (next_due_date+3 → tick → queued → worker → TG), dedup 0-yangi
  isbot, timezone chegara sinovi, tg_chat_id yo'q holatda xato. Kod darajasida
  tsikl to'liq ko'rinadi; isbot Mac'da `make smoke` + worker + TG akkaunt bilan.

## Keyingi sessiya nimadan boshlaydi
1. F1 3 teshikni yoz (yuqoridagi aniq patch) → `go build ./...` → `go test ./internal/client/...`
   → smoke'ga DELETE + notification bloki qo'sh → openapi'ni docs/ va mobile docs/ ga ko'chir.
2. F4 clock-injection sinovini yoz (notify schedule_test uslubida) + smoke isbot-jadval.
3. F2 widget test (sheet validatsiya + double-tap) — Mac'da.
4. Screenshot juftliklari (F3 list oldin/keyin, F2 tavsiya oqimi) — emulyatorda.
