# 2026-07-22 03:00 · fix-2 · REAL-QURILMA FIX 2 (H1–H4)

Tanqid qabul qilindi: o'tgan (fix-1) zichlik faqat dashboardga edi. Bu safar tizimli +
har ekran screenshot bilan. Emulyator yo'q → widget-render PNG (44px status-zona simulyatsiyasi).

## H1 — status bar / SafeArea (TIZIMLI)
**Ildiz sabab (2 ta):**
1. `main.dart` da `SystemUiOverlayStyle` / `SystemUiMode` YO'Q edi — zamonaviy Android
   (One UI/Android 15) edge-to-edge default, ilova status bar ortiga chizadi.
2. `AppShell` body'sida kontent uchun `SafeArea` YO'Q edi (faqat tabbar'da bor edi) →
   AppBar'siz shell ekranlar (dashboard/shogirdlar/kalendar/statistika/sozlamalar)
   kontenti status bar ostiga kirardi.

**Fix:**
- `main.dart`: `SystemChrome.setEnabledSystemUIMode(edgeToEdge)` + `setSystemUIOverlayStyle`
  (status ikonalar QORA — yorug' fon; navbar shaffof).
- `app_shell.dart`: body `SafeArea(bottom:false)` bilan o'raldi — barcha shell ekran bir joyda.
- Pushed ekranlar: student_profile/notifications AppBar yoki o'z SafeArea bilan allaqachon
  qoplangan (tekshirildi). Auth ekranlar o'z SafeArea.

**Isbot:** `screenshots/audit/` — har ekran 44px status-zona + qizil chegara chizig'i bilan;
kontent har doim chiziqdan PASTDA. Dasturiy tekshiruv: dashboard/shogirdlar/sozlamalar
sarlavhasi `top ≥ 44` assert (capture_screens.dart). 7 ekran vizual tasdiqlandi.

## H2 — davomad ishga tushirildi
**Ildiz sabab:** `showAttendanceSheet` DEFINED edi lekin HECH KIM chaqirmasdi (yetim) +
student profilda Davomad tab yo'q edi (faqat To'lovlar + Tavsiyalar).
**Fix:**
- Dashboard "Davomad" tezkor amali `context.go(students)` → `showAttendanceSheet(context)`.
- Student profil: 3-tab qo'shildi (To'lovlar / **Davomad** / Tavsiyalar). Davomad tab
  `_AttendanceTab` — `/students/{id}/attendance` → `Heatmap.fromDays`.
- `heatmap.dart`: level-0 rangi `rgba(255,255,255,.06)` (dark) → `c.glassHi` (light'da ko'rinsin).
**Sheet o'zi allaqachon to'liq ishlaydi** (checkbox, "N qoldi", bulk→backend, offline navbat T8).
**Test:** `attendance_test.dart` — sheet ochish → tanlash → Saqlash → `markBulk` studentId bilan.
**Backend/SQL isbot:** attendance endpoint (T6) mavjud; jonli SQL tekshiruv qo'lda (real backend kerak) — H4 yo'riqnomada.

## H3 — density HAMMA (asosiy) ekranga
- `students_screen`: screenH(20)→screenEdge(16); overdue sub money `withUnit`→`compact`.
- `stats_screen`: screenH→screenEdge (money allaqachon compact/money21).
- `settings_screen`: screenH→screenEdge; karta padding `x4l(20)`→`xxl(16)`.
- `student_profile`: (H2 tab) + mavjud density.
- Dashboard/Kalendar: fix-1 dan (regressiya screenshot bilan tekshirildi).
Har ekran `screenshots/audit/*.png`. Pul: `grep ",000" lib` = 0 (fix-1 dan).

## H4 — yakun
- `flutter analyze` 0; `flutter test --concurrency=1` **121/121**; `capture_screens.dart`
  10/10 (11 PNG, `screenshots/audit/`). `capture_screens.dart` `_test.dart` bilan tugamaydi →
  suite'da ishlamaydi (faqat qo'lda artefakt generatori) — ataylab.

## O'zgargan fayllar
lib: `main.dart`, `features/shell/ui/app_shell.dart`, `core/widgets/heatmap.dart`,
`features/dashboard/ui/dashboard_screen.dart` (Davomad wiring),
`features/students/ui/{student_profile_screen(Davomad tab),students_screen(density)}.dart`,
`features/stats/ui/stats_screen.dart`, `features/settings/ui/settings_screen.dart`.
test: `features/attendance_test.dart`(yangi), `capture_screens.dart`(kengaytirildi).

## Keyingi sessiya / qolgan
- H3 [~]: Tavsiyalar/Tarif shablonlari/dialog-toast — kod density-mos, lekin alohida
  screenshot YO'Q (BAJARILMADI). Kerak bo'lsa capture_screens.dart ga qo'shish.
- Dashboardda "Bugun N keldi" davomad ko'rsatkichi qo'shilmadi (ixtiyoriy).
- Real Samsung'da qo'lda: edge-to-edge + status ikona rangini tasdiqlash (H4 yo'riqnoma).
