# 2026-07-22 01:00 · fix-1 · REAL-QURILMA FIX (G1–G6)

Foydalanuvchi real telefonda uch muammo topdi: (A) hamma narsa HADDAN KATTA,
(B) kalendar ishlamayapti + to'lov holatlari kerak, (C) motivatsiya yo'q.
So'ralgan set: G1,G2,G3,G5,G6 (+G4 dan maqsad ringi). Barchasi bajarildi.

## Muhit kashfiyoti
- Bu sessiya foydalanuvchi Mac'ida (bulut EMAS) → `flutter analyze/test` ishladi.
- Emulyator/simulyator YO'Q (`flutter devices` bo'sh: Android SDK/Xcode yo'q) →
  screenshotlar widget-render PNG orqali (real fontlar yuklab, `screenshots/density/`).
- Kod allaqachon light/navy REDESIGN holatida edi (STATUS R-seriyasi eskirgan) —
  foydalanuvchi shuni sinab zichlik muammosini topgan.

## G1 — zichlik/tipografiya (token)
- `app_spacing.dart`: `screenEdge=16`, `cardPadDense=14`, `sectionGapDense=18` (D210).
- `app_text.dart`: `money20` (KPI 20px), `money15` (mini). Eski KPI `money24@27` OLIB TASHLANDI.
- `core/widgets/money_text.dart` YANGI — pul KO'RSATISHNING yagona komponenti: raqam +
  `so'm` ~57% soft suffiks (D211). `Money` util (probel, mln) O'ZGARMADI.
- `test/core/utils/money_test.dart` — eski `K/M` kutgan (RED edi) → `mln`ga to'g'rilandi.

## G2 — dashboard qayta kompozitsiya
`features/dashboard/ui/dashboard_screen.dart` to'liq qayta yozildi:
salom qatori + yig'ma hero (daromad money24 + maqsad ringi RingSize.profile + progress bar +
mini qarz/faol) + BUGUN (kechikkan/bugungi, "To'lov" tugma, bo'sh→"hammasi joyida ✓") +
tezkor 3 (To'lov/Shogird/Davomad) + so'nggi faoliyat (5, badge stacked).
Yig'im% backend D060 (`collected/expected_this_month`), fallback proksi (D209).

## G3 — kalendar + to'lov holatlari
- `calendar/providers/calendar_provider.dart`: `CalMonth/CalDay/CalEntry/CalStatus` (rich
  kontrakt: kun status + shogird yozuvlari + summary).
- `calendar/ui/calendar_screen.dart`: rang-kodli kunlar (unpaid→qizil, partial→sariq,
  paid→yashil, planned→neytral-ko'k, today→navy), kun sheeti (ism + "300 000 / 550 000" +
  badge + qoldiq-prefill "To'lov qo'shish"), oy xulosasi, filtr (all/paid/partial/unpaid).
- BACKEND (sub-agent, D057): `GET /calendar?month=`. **D058: qisman-to'lov BUG FIX** —
  eski `ComputePeriod` har to'lovda `next_due_date` siljitardi; endi davr to'liq
  yopilmaguncha siljimaydi (table-driven testlar).

## G4 (qisman — talab qilinmagan)
- `monthly_goal`: Me DTO + MeUpdate + `goal_sheet.dart` (PATCH /me). Backend D059.
- Migratsiya 0017 allaqachon mavjud edi.
- **Milestone toast OLIB TASHLANDI** — widget testda Timer/postFrame fragilligi
  cheksiz hangga sabab bo'ldi; G4 talab qilinmagan (D213). Streak/digest ham qurilmadi.

## G5 — audit (`DESIGN_AUDIT3.md`)
- `grep -rn ",000\|NumberFormat" lib` → topildi `settings_screen.dart:612` `'50,000'`
  (aynan foydalanuvchi shikoyati!) → `Money.format(50000)` [FIXED]. Vergulli pul 0 ta.
- Shogirdlar/Profil/Stats/Bildirishnoma — eski token (funksional), follow-up ko'chirish.

## G6 — yakuniy tekshiruv
- `flutter analyze` → **0 issue**.
- `flutter test` → **120/120** (+ `capture_screens.dart` 2 ta). Yangi: dashboard_density (2),
  calendar (1). Tuzatilgan RED'lar: money mln, students filtr (redesign `_FilterTab`, AppChip emas).
- **Adaptivlik**: 360/393/430 × 1.0/1.3 → overflow 0. Yo'lda topilgan+tuzatilgan overflowlar:
  `_MiniStat` label, goal-prompt, `_ActivityRow` (money+badge → vertikal Column), kalendar `_legend`.
  Overflow cascade "hang" ko'rinishida edi — asl sabab RenderFlex overflow (fat test font).
- Screenshotlar (real font): `screenshots/density/dashboard.png`, `calendar.png`.

## O'zgargan fayllar (mobile)
lib: `core/theme/{app_text,app_spacing}.dart`, `core/widgets/money_text.dart`(yangi),
`core/i18n/strings.dart`, `core/api/dto/{auth_dto,dashboard_dto}.dart`(+regen `.g.dart`),
`features/dashboard/ui/{dashboard_screen,goal_sheet(yangi)}.dart`,
`features/calendar/{providers/calendar_provider,ui/calendar_screen}.dart`,
`features/settings/ui/settings_screen.dart`(1 qator), `features/recommendations/*`(lint).
test: `dashboard_density_test`(yangi), `calendar_test`(yangi), `capture_screens.dart`(yangi),
`core/utils/money_test.dart`(fix), `features/students_screen_test.dart`(fix).
docs: `DESIGN_AUDIT3.md`(yangi).

## Keyingi sessiya
1. G5 follow-up: qolgan ekranlarni dense tokenlarga ko'chirish (audit §4).
2. G4 to'liq (agar so'ralса): streak (backend agregat kerak) + haftalik digest (notify oqimi).
3. Milestone toast — kerak bo'lsa test-xavfsiz qayta qo'shish (goal-set sheet ichida, dashboard build'da emas).
4. Real qurilmada qo'lda tekshirish: dashboard→kalendar(3 rang, kun sheet, qoldiq-prefill)→maqsad qo'yish.
