# 2026-07-20 21:00 · mobile · T1 (design system)

## Nima qilindi

### 0. Sessiya infratuzilmasi (bo'lmagan)

`trainer-mobile` repo'sida `.claude/state/` UMUMAN yo'q edi (monorepodan
ajratishda ko'chmagan). Yaratildi: `SYSTEM.md`, `STATUS.md`, `DECISIONS.md`,
`journal/`. STATUS.md T0–T9 jadvali + bloker ro'yxati bilan to'ldirildi.

### 1. MUHIT BLOKERI — Flutter umuman yo'q edi

`flutter: command not found`. T0 "tugallangan" deb belgilangan, lekin bu
mashinada birorta darvoza (`analyze`/`test`/`build`) hech qachon ishlamagan —
`pubspec.lock` boshqa muhitda generatsiya qilingan.

Hal qilindi (D101): `git clone -b stable` → `~/flutter`.
**Flutter 3.44.6 · Dart 3.12.2** — `pubspec.yaml` dagi `sdk: ^3.12.2` ga mos.
`flutter pub get` muvaffaqiyatli, darvozalar endi haqiqatan ishlaydi.

### 2. T1 — design system

Manba: `design/ustoz-v2.1-tavsiyalar.html` (916 qator) to'liq o'qildi.
Tema nomi: **"KECHKI ZAL"** — dark iron · chalk · anor glow.

**Tokenlar** (`lib/core/theme/`):
- `app_colors.dart` — `AppColors` `ThemeExtension` sifatida, 25 token +
  3 gradient. `context.colors` extension.
- `app_text.dart` — 30 uslub. Unbounded (display) / Manrope (UI) /
  JetBrainsMono (pul, `tabularFigures`).
- `app_spacing.dart` — `AppSpacing`, `AppRadius`, `AppDuration`.
- `app_motion.dart` — dizayndagi ATIGI 2 ta egri chiziq:
  `house` = `cubic-bezier(.2,.8,.2,1)`, `sheet` = `cubic-bezier(.2,.9,.25,1)`.
- `app_theme.dart` — Material ripple O'CHIRILGAN (dizaynda yo'q, bosilish
  faqat `scale(.97)`).

**Komponentlar** (`lib/core/widgets/`, 16 fayl):
`PressScale` · `GlassCard` (+`.bare`/`.hero`) · `GradientButton` ·
`GhostButton` · `GhostIconButton` · `StatusBadge` · `AppChip` + `AppChipRow` ·
`Avatar` (+`.small`) · `PlitaRing` (CustomPainter) · `StudentCard` ·
`SectionHeader` + `AppSection` · `ListRow` + `ListRowGroup` · `EmptyState` ·
`AppToast` · `Heatmap` · `MiniBarChart` · `Timeline` + `TimelineEvent` ·
`AppSheetShell` + `showAppSheet`.

`lib/core/utils/money.dart` — `format` / `withUnit` / `compact`.
Ajratgich **uzilmas probel** (U+00A0). Pul har joyda `int`.

`lib/dev/gallery.dart` — hamma komponent har holatida. Router'da
`kDebugMode && Env.devTools` sharti bilan — release'da tree-shake bo'ladi.

`main.dart` — `AppTheme.dark` ulandi, `textScaler` 1.0–1.3 ga qisildi.

## Qarorlar (DECISIONS.md)

- **D101** Flutter SDK `~/flutter` ga
- **D102** `POST /payments/preview` backendga qo'shiladi (T6 da) — hozircha yozuv
- **D103** `debt` rangi token sifatida rasmiylashtirildi (dizaynda o'zgaruvchi yo'q edi)
- **D104** Ring gradienti (`#FF6A3D`) tugma gradientidan (`#FF5340`) FARQ QILADI — ikkalasi saqlandi
- **D105** `backdrop-filter` kartalarda qo'llanmaydi (jank byudjeti)
- **D106** Sheet scrim'i blur'siz
- **D107** `StudentCard` badge ustuni 45% bilan cheklanadi

## Topilgan va tuzatilgan xato

Galereyaning adaptivlik testi `360×640 @ textScale 1.3` da **haqiqiy
overflow** topdi: `StudentCard` ning badge ustuni flex emas → RenderFlex
unga cheksiz kenglik beradi → "3 KUN KECHIKDI" qatorni 3.8px yorib chiqdi.
`LayoutBuilder` + `ConstrainedBox(maxWidth: 45%)` bilan tuzatildi (D107).

Bu test avval yolg'on yashil edi: galereya `ListView`, ekrandan tashqaridagi
bolalar umuman qurilmaydi — test faqat birinchi ekranni tekshirayotgan edi.
Endi ro'yxat oxirigacha aylantiriladi.

## Darvozalar

```
dart format      → toza
flutter analyze  → No issues found!
flutter test     → All tests passed! (22 ta)
```

Testlar: `money_test.dart` (16), `avatar_test.dart` (6),
`gallery_smoke_test.dart` (1 + 6 adaptivlik matritsasi), `widget_test.dart` (1).

## Chala qolgani / keyingi sessiya

**T2 — API qatlami + auth infra** dan boshlanadi (STATUS.md ga yozilgan).

### OCHIQ BLOKERLAR (foydalanuvchi harakati kerak)

- **B2 · Xcode yo'q** (faqat Command Line Tools) → iOS build MUMKIN EMAS.
  App Store'dan Xcode o'rnatilishi kerak (~15GB). CLI orqali hal qilinmaydi.
- **B3 · JDK + Android SDK yo'q** → Android APK release build (T9) mumkin emas.
  `brew install --cask temurin` + `cmdline-tools` bilan hal qilinadi.

Ya'ni T0 ning "ikkala platformada build bo'ladi" DoD i hozircha
TASDIQLANMAGAN — kod darajasida tayyor, lekin platforma toolchain'i yo'q.

### Kontrakt kamchiligi (T6 da hal qilinadi)

`docs/openapi.yaml` to'liq tekshirildi — **`POST /payments/preview` YO'Q**.
Yagona yo'l: `POST /payments` qilib `PaymentCreated.student.next_due_date`
o'qish, ya'ni to'lovni haqiqatan saqlash. T6 ga kelganda BACKEND KAMCHILIGI
protokoli bo'yicha `trainer-back` ga kirib qo'shiladi (D102, STATUS X1).
</content>
