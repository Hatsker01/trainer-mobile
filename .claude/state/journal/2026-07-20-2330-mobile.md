# 2026-07-20 23:30 · mobile · T2–T9 + YAKUNIY HISOBOT

Bitta uzun sessiyada T2 dan T9 gacha bajarildi. T0/T1 avvalgi yozuvda.

## Bajarilgan ishlar (qisqacha)

- **T2** API qatlami: DTO'lar (json_serializable, `format: date` va
  `date-time` farqli), Dio client, AuthInterceptor (401→single-flight
  refresh, `Completer` qulfi), ErrorInterceptor (AppException), telefon
  mask'lovchi log interceptor, secure token storage, repozitoriylar.
- **T3** auth: 3 slayd onboarding, telefon (+998 niqob), OTP (bitta
  yashirin TextField — paste/autofill, silkinish, 60s taymer), profil
  sozlash. Router sessiya asosida redirect.
- **T4** dashboard: hero ring, bugungi to'lov (✓/🔔), skeleton
  (shimmersiz pulse), SWR kesh, 4 holat.
- **T5** shogirdlar: ro'yxat (300ms debounce qidiruv, cheksiz scroll,
  server tartibi), profil (heatmap, tablar), qo'shish (tarif shablon,
  422 field, invite share_plus), arxivlash.
- **T6** to'lov: Idempotency-Key (bir marta gen, retry'da o'zgarmas),
  double-tap himoya, jonli minglik. Davomad: bulk, "N qoldi".
- **T7** stats/sozlamalar: KPI+grafik (tap→oy), til (darhol), tarif
  CRUD, remind_time, TG invite, chiqish.
- **T8** offline: dashboard/shogirdlar SWR kesh, davomad outbox
  (connectivity qaytganda auto-flush, idempotent), offline banner.
  To'lov offline QILINMAYDI (D110 — pul jonli).
- **T9** release: Android APK (obfuscate + split-per-abi), adaptivlik
  matritsasi testi, README, hisobot.

## YAKUNIY HISOBOT

### T0–T9 jadval
Hammasi `[x]` — STATUS.md ga qarang.

### O'lchovlar
- **Kod:** 67 Dart fayl (`.g.dart`siz), ~11 500 qator.
- **Testlar:** **97 ta, hammasi yashil.** Taqsimot: formatters/avatar (22),
  API error mapping (24) + refresh flow (7), auth flow (11), students
  filtr/qidiruv (6), to'lov double-tap+Idempotency (4), outbox (6),
  adaptivlik matritsasi (24), gallereya (7+ ichida).
- **`flutter analyze`:** **No issues found** (qattiq lintlar bilan).
- **APK (release, obfuscate, split-per-abi):**
  - arm64-v8a: **18.4 MB** (maqsad <20MB ✓)
  - armeabi-v7a: 15.6 MB
  - x86_64: 19.8 MB
- **`flutter build web`:** o'tadi (to'liq kompilyatsiya tekshiruvi).

### Performance
- `PlitaRing.shouldRepaint` faqat qiymat/ohang o'zgarganda.
- `GlassCard` da `BackdropFilter` default O'CHIQ (D105) — scroll jank'ini
  oldini oladi. Blur faqat tabbarda (ustidan kontent suzadi).
- Skeleton — pulse opacity (shader shimmer emas).
- `const` qamrovi: `prefer_const_*` xato darajasida majburlangan.
- **DevTools trace olinmadi** — bu real qurilma/emulyator talab qiladi
  (headless muhitda profil rejimida ishga tushirib bo'lmadi). Kod
  darajasidagi byudjet choralari qo'llangan.

### Adaptivlik (T9 matritsasi)
Avtomatik test: Dashboard, Shogirdlar, Statistika, Sozlamalar × {360×640,
393×852, 430×932} × {textScale 1.0, 1.3} = 24 holat, overflow NOLGA teng.
`textScaler` global 1.0–1.3 ga qisilgan (`main.dart`).

### BACKEND'GA KIRILDI
**Kirilmadi.** `docs/openapi.yaml` mavjud kontrakt bilan hamma ekran
qurildi. `POST /payments/preview` haqidagi dastlabki reja (D102) BEKOR
QILINDI — `PaymentCreated.student.next_due_date` serverdan kelgani uchun
keyingi to'lov sanasi saqlagandan keyin toast'da ko'rsatiladi (yagona
haqiqat manbai baribir backend). Saqlashdan oldingi jonli preview TODO
bo'lib qoldi (backend repo bu sessiyada ajratilmagan).

### OCHIQ BLOKER (foydalanuvchi harakati)
- **B2 · Xcode yo'q** (faqat CLT). iOS uchun Dart/engine qismi
  kompilyatsiya bo'ldi, lekin Xcode app-build qismi ("Application not
  configured for iOS") to'liq Xcode talab qiladi. Kod tayyor, build
  tasdig'i Xcode o'rnatilgach mumkin.

### Muhit (hal qilingan)
- Flutter `~/flutter` (3.44.6). Android: Homebrew OpenJDK 17 +
  `android-commandlinetools` (SDK 36) — `flutter doctor` da Android
  toolchain ✓, APK qurildi.
- Ishga tushirish uchun har terminalda:
  ```
  export PATH="$HOME/flutter/bin:$PATH"
  export JAVA_HOME=/opt/homebrew/opt/openjdk@17
  export ANDROID_SDK_ROOT=/opt/homebrew/share/android-commandlinetools
  ```

### QO'LDA TEKSHIRISH STSENARIYSI (real qurilma, 10 qadam)
1. `flutter run --dart-define=API_URL=<backend>` — onboarding 3 slayd,
   "Boshlash" → telefon.
2. `+998` bilan raqam kirit → "Davom etish" → OTP ekrani, kod kelishi
   (TG/SMS matni), 60s taymer ketadi.
3. 6 xonali kodni kirit (yoki noto'g'ri kod → kataklar silkinadi/tozalanadi).
4. Birinchi kirish bo'lsa profil (ism majburiy) → Saqlash → dashboard.
5. Dashboard: hero ring, "Bugun to'lov" kartasi → "✓ To'landi" → sheet,
   summa prefill, usul tanla, Saqlash → toast "Keyingi to'lov: …".
6. "🔔 Eslatish" → toast (yoki "bugun 3-marta" warning).
7. Shogirdlar tab → qidiruv (300ms), filtr chiplar (Qarzdor), pastga
   scroll (keyingi sahifa).
8. Shogird kartasi → profil: heatmap, to'lov tarixi, tahrirlash/arxivlash.
9. `+` (markaz) → davomad sheet, bir nechta belgila → Saqlash.
   **Airplane rejim:** oldin ro'yxat ochiladi (kesh), davomad belgilash →
   banner "Internet yo'q — saqlandi", rejimni o'chir → avtomatik yuboriladi.
10. Sozlamalar → tilni Русский ga o'zgart (BARCHA matn darhol almashadi) →
    tarif shablon qo'sh → Chiqish (confirm) → login ekraniga qaytadi.

## Keyingi sessiya (agar davom etsa)
- Xcode o'rnatilgach iOS release build tasdig'i.
- Backend repo ajratilsa: `POST /payments/preview` + sheetda jonli preview.
- Real qurilmada DevTools performance trace + APK'ni haqiqiy release
  kaliti bilan imzolash (hozir debug key).
</content>
