<?xml version="1.0" encoding="utf-8"?>
<!-- FAQAT DEBUG/PROFILE build uchun.

     Dev serverga real telefondan ulanish: API_URL har safar boshqa LAN IP
     bo'ladi (192.168.x.x, 172.20.x.x, hotspot...). Har birini release
     konfiguratsiyasiga qo'shib yurish mumkin emas.

     Release build bu faylni KO'RMAYDI — u `src/main/res/xml/` dagi
     qattiq ro'yxatni ishlatadi (faqat prod IP + emulyator ko'prigi).
     Gradle `src/debug` ni faqat debug variantiga qo'shadi.  -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
